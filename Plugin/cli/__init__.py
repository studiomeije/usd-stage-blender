# USD Stage for Blender CLI — bundled command-line interface.
#
# This package ships inside the Blender addon so no separate install is needed.
# Run with:  python3 /path/to/Plugin <command> [options]
