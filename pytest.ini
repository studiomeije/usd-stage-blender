[pytest]
# scripts/tests holds the release-archive suite (unittest-style, which pytest
# collects).
testpaths = tests scripts/tests
addopts = --strict-markers
markers =
    integration: requires Blender executable (skipped if unavailable)
